{
  "name": "@starter/llm",
  "version": "2.0.0",
  "description": "LLM prompt service — structured AI interactions with optimized prompt templates",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "docker": "docker compose up --build -d && KEEP_RUNNING=1 ./scripts/docker-up-build-test.sh",
    "test:watch": "vitest",
    "test:docker": "bash scripts/docker-test.sh",
    "test:integration": "bash scripts/integration-test.sh"
  },
  "dependencies": {
    "@elevenlabs/elevenlabs-js": "^2.35.0",
    "@google/generative-ai": "^0.24.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.1",
    "ioredis": "^5.4.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^1.6.0"
  }
}
